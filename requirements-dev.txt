# Development dependencies
# Compile with: pip-compile requirements-dev.in -o requirements-dev.txt
# Or: uv pip compile requirements-dev.in -o requirements-dev.txt

# Include base dependencies with constraints -r requirements.txt -c requirements.txt

# Development dependencies
pytest
pytest-cov
pytest-mock
pytest-xdist
