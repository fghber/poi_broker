[tool.coverage.run]
relative_files = true
branch = true
omit = [
    'tests/*',
    '.tox/*',
]

[tool.pytest.ini_options]
addopts = "--cov=poi_broker --cov-report=xml --cov-report=term"
testpaths = [
    "tests",
]
